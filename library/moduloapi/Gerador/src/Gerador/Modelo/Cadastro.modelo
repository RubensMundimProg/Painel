<div class="page-head">
    <h2>Cadastro</h2>
</div>
<div class="cl-mcont">
<div class="row">
    <div class="block-flat">
        <div class="content">
    <?php

                $form->setAttribute('action', $this->url('application', array('controller'=>$controller,'action' => 'gravar')));
                $form->setAttribute('class', 'form-horizontal');
                $form->setAttribute('data-role', 'form');
                $form->prepare();
                echo $this->form()->openTag($form);
                echo $this->formHidden($form->get('Id'));
                ?>

                {{campos}}
                <div class="form-group">
                    <div class="col-md-10">
                        <button type="submit" class="btn btn-default">Salvar</button>
                        <a href="<?=$this->url('application', ['controller'=>$controller])?>" class="btn btn-warning">Voltar</a>
                    </div>
                </div>
                <?= $this->form()->closeTag(); ?>
        </div></div>
    </div>
</div>